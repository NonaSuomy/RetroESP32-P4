/* Minimal PrBoom type compatibility used by the standalone Duke MIDI parser. */
#ifndef DUKE_PAPP_DOOMTYPE_H
#define DUKE_PAPP_DOOMTYPE_H

#include <stddef.h>
#include <stdint.h>

typedef unsigned int boolean;
typedef uint8_t byte;

#endif
